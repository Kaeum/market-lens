spring:
  r2dbc:
    url: r2dbc:h2:mem:///testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:

  flyway:
    enabled: false

  data:
    redis:
      host: localhost
      port: 6379

kafka:
  bootstrap-servers: localhost:9092

kis:
  app-key: test-key
  app-secret: test-secret
  account-number: test-account
  base-url: https://test.kis.com

krx:
  api-key: test-krx-key
  base-url: https://test.krx.co.kr

logging:
  level:
    io.kaeum.marketlens: DEBUG
